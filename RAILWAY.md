# نصب MirzaBot روی Railway

این نسخه برای اجرای مستقیم روی Railway آماده شده است و به VPS، Apache، SSL دستی یا Cron جداگانه نیاز ندارد. دیتابیس MySQL، فایل‌های متغیر روی Volume، زمان‌بندی‌های یک‌دقیقه‌ای، وب‌هوک امن و انتقال کامل اطلاعات در آن پشتیبانی می‌شود.

## چیزهایی که لازم دارید

- اکانت GitHub و Railway
- توکن ربات از BotFather
- عدد Telegram ID مدیر اصلی
- نام کاربری ربات بدون `@`

## ۱. ساخت پروژه

1. مخزن را در GitHub خودتان Fork کنید.
2. در Railway روی **New Project** بزنید.
3. گزینه **Deploy from GitHub repo** را انتخاب کنید و همین Fork را بدهید.
4. داخل همان پروژه روی **New → Database → MySQL** بزنید.

## ۲. افزودن Volume

1. سرویس MirzaBot را باز کنید.
2. در بخش **Volumes** یک Volume بسازید.
3. مسیر Mount را دقیقاً `/data` قرار دهید.
4. تعداد Replica سرویس را روی `1` نگه دارید.
5. حالت Serverless/Sleep را خاموش نگه دارید؛ زمان‌بندی‌های ربات باید همیشه فعال باشند.

بدون Volume نیز برنامه موقتاً بالا می‌آید، اما فایل‌های نمایندگی و فایل‌های قابل‌تغییر پس از Deploy بعدی از بین می‌روند.

## ۳. تنظیم Variables

در سرویس MirzaBot، بخش **Variables**، موارد زیر را اضافه کنید:

| نام | مقدار |
|---|---|
| `MYSQL_URL` | Reference به `MYSQL_URL` سرویس MySQL، معمولاً `${{MySQL.MYSQL_URL}}` |
| `BOT_TOKEN` | توکن کامل BotFather |
| `OWNER_ID` | Telegram ID عددی مدیر اصلی |
| `BOT_USERNAME` | نام کاربری ربات بدون `@`؛ اختیاری است ولی بهتر است وارد شود |
| `TELEGRAM_WEBHOOK_SECRET` | یک رشته تصادفی ۳۲ تا ۶۴ کاراکتری فقط با حروف، عدد، `_` یا `-` |
| `BACKUP_SECRET` | یک رمز تصادفی و متفاوت، حداقل ۲۴ کاراکتر |
| `MIRZA_DATA_DIR` | `/data/mirzabot` |

اگر نام سرویس دیتابیس شما `MySQL` نیست، Reference متغیر را از رابط Railway انتخاب کنید تا نام درست خودکار درج شود.

`TELEGRAM_WEBHOOK_SECRET` و `BACKUP_SECRET` را شبیه هم و برابر توکن ربات نگذارید. این مقادیر نباید در GitHub یا پیام عمومی قرار بگیرند.

## ۴. ساخت دامنه و راه‌اندازی

1. در سرویس MirzaBot به **Settings → Networking** بروید.
2. روی **Generate Domain** بزنید.
3. پس از ساخته‌شدن دامنه، یک **Redeploy** یا **Restart** انجام دهید.
4. در Deploy Logs باید پیام `MirzaBot bootstrap complete` را ببینید.
5. آدرس زیر را باز کنید:

```text
https://دامنه-شما/railway/health.php
```

اگر `status` برابر `ok` بود، در تلگرام برای ربات `/start` بفرستید. جدول‌های دیتابیس و وب‌هوک ربات اصلی هنگام شروع سرویس خودکار ساخته و ثبت می‌شوند.

برای دامنه شخصی، متغیر `APP_DOMAIN` را برابر دامنه بدون `https://` قرار دهید و دوباره Redeploy کنید. در نبود آن، دامنه عمومی Railway خودکار استفاده می‌شود.

## بک‌آپ کامل

1. آدرس زیر را باز کنید:

```text
https://دامنه-شما/railway/transfer.php
```

2. با مقدار `BACKUP_SECRET` وارد شوید.
3. روی **دانلود بک‌آپ کامل** بزنید.
4. فایل ZIP را در جای امن نگه دارید.

این ZIP شامل دیتابیس، ربات‌های نمایندگی، فایل‌های داده، تصویر QR، توکن پنل وب و صف‌های نیمه‌تمام است. فایل بسیار حساس است و می‌تواند شامل توکن ربات‌های نمایندگی و اطلاعات کاربران باشد.

بک‌آپ خودکار قدیمی MirzaBot که به کانال گزارش فرستاده می‌شود نیز فعال می‌ماند، ولی برای جابه‌جایی کامل بین دو اکانت از ZIP صفحه Transfer استفاده کنید.

## انتقال به اکانت Railway جدید

ترتیب امن انتقال:

1. در اکانت قدیمی ابتدا از صفحه Transfer فایل ZIP را دانلود کنید.
2. پروژه جدید را طبق مراحل بالا با MySQL و Volume جدید بسازید.
3. همان `BOT_TOKEN`، `OWNER_ID` و در صورت امکان همان `BACKUP_SECRET` را در پروژه جدید تنظیم کنید.
4. دامنه جدید را بسازید و صبر کنید سرویس Healthy شود.
5. صفحه Transfer دامنه جدید را باز کنید و وارد شوید.
6. ZIP را انتخاب کنید، عبارت `RESTORE` را دقیق بنویسید و دکمه ایمپورت را بزنید.
7. پس از پیام موفقیت، سرویس جدید را یک‌بار Restart کنید.
8. ربات اصلی و یک ربات نمایندگی را آزمایش کنید.
9. سرویس قدیمی را خاموش نگه دارید تا دوباره وب‌هوک را به دامنه قدیمی برنگرداند.

هنگام ایمپورت، ربات و کارهای زمان‌بندی موقتاً متوقف می‌شوند. قبل از جایگزینی، یک بک‌آپ اضطراری داخل `/data/mirzabot/backups` ساخته می‌شود و سه نسخه آخر نگهداری می‌شود.

اگر به‌جای توکن قبلی از یک ربات Telegram کاملاً جدید استفاده کنید، رکورد کاربران منتقل می‌شود ولی کاربران باید ابتدا ربات جدید را Start کنند؛ Telegram اجازه ارسال پیام از رباتی را که کاربر آغاز نکرده نمی‌دهد.

## ربات‌های نمایندگی

- پوشه‌ها و داده‌های تمام ربات‌های نمایندگی روی Volume نگهداری می‌شود.
- پس از Deploy یا Restore، کد هر نمایندگی با نسخه جاری هماهنگ می‌شود، ولی توکن، محصولات، داده کاربران و متن‌های سفارشی آن حفظ می‌شود.
- وب‌هوک همه نمایندگی‌ها پس از شروع سرویس و پس از Restore خودکار روی دامنه جدید ثبت می‌شود.

## رفع خطاهای رایج

### Deploy روی Healthcheck متوقف می‌شود

- وجود `MYSQL_URL` و Reference صحیح دیتابیس را بررسی کنید.
- `BOT_TOKEN` و `OWNER_ID` نباید خالی باشند.
- `TELEGRAM_WEBHOOK_SECRET` فقط می‌تواند شامل حروف، عدد، `_` و `-` باشد.
- Mount Path ولوم باید `/data` باشد.

### ربات پیام را جواب نمی‌دهد

- پس از Generate Domain حتماً Restart/Redeploy کنید.
- در Logs وضعیت main webhook باید `ok` باشد.
- توکن BotFather و دامنه `APP_DOMAIN` را دوباره بررسی کنید.
- هم‌زمان سرویس قدیمی را Restart نکنید؛ هر ربات Telegram فقط یک Webhook فعال دارد.

### صفحه انتقال باز می‌شود ولی ورود ممکن نیست

- `BACKUP_SECRET` باید حداقل ۲۴ کاراکتر باشد.
- بعد از تغییر Variable سرویس را Redeploy کنید.
- رمز را با فاصله ابتدا یا انتها کپی نکنید.

### فایل بک‌آپ بزرگ است

حد آپلود پیش‌فرض این Fork حدود ۵۱۲ مگابایت است. اگر ZIP بزرگ‌تر شد، ابتدا فایل‌های غیرضروری ربات‌های نمایندگی را پاک‌سازی کنید یا از Railway CLI/Volume download برای انتقال فایل‌های بسیار بزرگ استفاده کنید.

## نکات امنیتی

- صفحه Transfer را فقط هنگام نیاز باز کنید و رمز آن را دوره‌ای عوض کنید.
- ZIP بک‌آپ را در فضای عمومی آپلود نکنید.
- Variableها را داخل فایل‌های Git commit نکنید.
- یک Replica اجرا کنید؛ چند Replica باعث اجرای چندباره زمان‌بندی‌ها می‌شود.
- از قابلیت Backup خود Railway برای MySQL و Volume نیز به‌عنوان لایه دوم حفاظت استفاده کنید.
